#!/bin/bash
# shellcheck disable=SC2034

plugin_cmd="yamllint -s" # never trigger error, just display issues

# Dependencies:
# Alpine: available via apk yamllint

# file extensions this plugin handles
plugin_extensions=(
    .yml
)

# file-types this plugin handles
plugin_file_types=(
)
